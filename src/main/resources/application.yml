server:
  port: 8080
  servlet:
    context-path: /api

spring:
  profiles:
    active: @spring.profiles.active@

management:
  endpoints:
    web:
      exposure:
        include: '*'
        # include: health,prometheus,metrics